function setup() {
    createCanvas(windowWidth, windowHeigth);
  }
  
  //console.log("message");

  function draw() {
    /*
    //fill in background
    fill(240);
    rect(0, 0, windowWidth/2, windowHeight/2);
    fill(120);
    rect(windowWidth/2, 0, windowWidth/2, windowHeight/2);
    fill(60);
    rect(0, windowHeight/2, windowWidth/2, windowHeight/2);
    fill(0);
    rect(windowWidth/2, windowHeight/2, windowWidth/2, windowHeight/2);

    //DRAW CURVES
    noFill(0);
      //top left
    stroke(0);
    bezier(windowWidth/2, windowHeight/2, 0, 0, windowWidth/2, 0, windowWidth/2, windowHeight/2);
    bezier(windowWidth/2, windowHeight/2, 0, 0, 0, windowHeight/2, windowWidth/2, windowHeight/2);
      //top right
    stroke(60);
    bezier(windowWidth/2, windowHeight/2, windowWidth/2, 0, windowWidth, 0, windowWidth/2, windowHeight/2);
    bezier(windowWidth/2, windowHeight/2, windowWidth, 0, windowWidth, windowHeight/2, windowWidth/2, windowHeight/2);
      //bottom left
    stroke(120);
    bezier(windowWidth/2, windowHeight/2, 0, windowHeight/2, 0, windowHeight, windowWidth/2, windowHeight/2);
    bezier(windowWidth/2, windowHeight/2, 0, windowHeight, windowWidth/2, windowHeight, windowWidth/2, windowHeight/2);
      //bottom right
    stroke(240);
    bezier(windowWidth/2, windowHeight/2, windowWidth/2, windowHeight, windowWidth, windowHeight, windowWidth/2, windowHeight/2);
    bezier(windowWidth/2, windowHeight/2, windowWidth, windowHeight/2, windowWidth, windowHeight, windowWidth/2, windowHeight/2);

    */

    background(225, 50, 200);
    

  }